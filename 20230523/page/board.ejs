<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>board</title>
    <style>
        body {
            padding: 0; margin: 0;
            background-color: aliceblue;
        }
        .wrap {
            width: 100vw; height: 100%;
            display: flex; flex-direction: row;
        }
        .board {
            width: 100%; height: 100%;
            border: 1px solid;
        }
        .board_view {
            width: 100%; height: calc(100vh - 300px);
            border: 1px solid;
            padding: 10px;
            box-sizing: border-box;
            overflow-y : scroll;
        }
        ul {
            width: 90%;
        }
        li {
            list-style: none;
            display: flex;
        }
        li > div {
            width: 200px; height: 50px;
            box-sizing: border-box;
            border-bottom: 1px solid;
            display: flex;
            justify-content: center; align-items: center;
        }
        .list_no, .list_name, .list_del, .list_up {
            width: 10%;
        }
        .list_title {
            width: 20%
        }
        .list_detail, .list_content {
            width: 40%
        }
        .board_write {
            width: 100%; height: 300px;
            border: 1px solid;
            padding: 20px;
            box-sizing: border-box;
        }

        input, textarea {
            width: 500px;
        }
    </style>
</head>
<body>
    <div class="wrap">
        <div class="board">
            <div class="board_view">
                <ul>
                    <li class="list_top">
                        <div class="list_no">No</div>
                        <div class="list_title">Title</div>
                        <div class="list_detail">Detail</div>
                        <div class="list_name">Name</div>
                        <div class="list_del">Delete</div>
                        <div class="list_up">Update</div>
                    </li>
                </ul>
                <ul>
                    <% data.forEach((el, index) => { %>
                    <li>
                        <div class="list_no"><%= index+1 %></div>
                        <div class="list_title"><a href="/board/commentView/<%= el.id %>"><%= el.title %></a></div>
                        <div class="list_detail"><%= el.detail %></div>
                        <div class="list_name"><%= el.name %></div>
                        <div class="list_del"><a href="/board/delete/<%= el.id %>">Delete</a></div>
                        <div class="list_up"><a href="/board/update/<%= el.id %>">Update</a></div>
                    </li>
                    <% }); %>
                </ul>
            </div>
            <div class="board_write">
                <form action="/board" method="post">
                    <label for="">Title</label><br>
                    <input type="text" name="title"><br>
                    <label for="">Detail</label><br>
                    <textarea name="detail" id="" cols="30" rows="10"></textarea><br><br>
                    <button>Submit</button>
                </form>
            </div>
        </div>
    </div>
</body>
</html>