<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>board</title>
</head>
<body>
    <h2>Board page</h2>
    <h3>Writer : <%= data.name %></h3>

    <ul>
        <% data.Posts.forEach((post) => { %>
        <li>
            <p><%= post.msg %></p>
            <form action="/board/update_board/<%= post.id %>" method="post">
                <input type="text" name="msg">
                <button>Update</button>
            </form>
            <a href="/board/delete/<%= post.id %>">Delete</a>
        </li>
        <% }); %>
    </ul>
</body>
</html>